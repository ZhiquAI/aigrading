const HomePage = () => {
  return (
    <main style={{ padding: 24, fontFamily: "sans-serif" }}>
      <h1>AI Grading Platform V2 API</h1>
      <p>Use <code>/api/health</code> for health checks.</p>
    </main>
  );
};

export default HomePage;
